<!-- Threat-model context (task 005). The rendered summary is supplied at
     prompt-build time via PromptContext.threatModel (assembleScanPromptContext);
     when no threat_model.json exists yet the {{THREAT_MODEL}} slot renders the
     neutral "(none)" sentinel. -->
<threat_model>
Engagement threat model — the prioritized abuse cases, trust boundaries, and
crown-jewel assets the threat-model phase derived from recon. Use it to FOCUS
this round: pursue the highest-priority threats first, weight findings that touch
the crown-jewel assets, and treat the named trust boundaries as the lines an
attacker must cross. This is guidance, not a ceiling — report anything you find,
but lead with what the model says matters most. "(none)" means no model was
available; fall back to the recon and pre-recon deliverables.
{{THREAT_MODEL}}
</threat_model>
