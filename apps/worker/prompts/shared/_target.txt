Primary origin: {{WEB_URL}}

Reachable surface — AUTHORITATIVE (live origins recon observed on this target):
{{TARGET_SURFACE}}

Each finding's REAL attack surface is at the origin above that actually serves it
— frequently a DISTINCT PORT (e.g. a REST API on :8080, an auth/OIDC service on
:8090), NOT a path under {{WEB_URL}}. Map each endpoint to its service origin
using the recon deliverable's "Open Ports & Services" / "Network Surface" section,
and probe it THERE.

READ BEFORE CONCLUDING ANYTHING IS UNREACHABLE:
- {{WEB_URL}} (the primary origin) often serves only a single-page app (SPA) that
  returns `index.html` for ANY unknown path. If a probe to `{{WEB_URL}}/<api-path>`
  returns the SPA shell / index.html / a static-server 404 or 405, you are hitting
  the SPA, NOT the API — switch to the API origin in the surface above (e.g. the
  `:8080` origin) and probe the same path there.
- Do NOT assume the API is reverse-proxied under {{WEB_URL}}/api/. That is only ONE
  possible topology; the surface above is ground truth. ONLY when the surface lists
  no separate API origin (or is "(none)" because recon has not run yet) should you
  fall back to probing `{{WEB_URL}}/api/`, `/api/v1/`, `/api/health`, `/graphql` on
  the primary origin.
- Mark a finding VALIDATION_BLOCKED / unreachable ONLY after probing it against the
  correct service origin from the surface above and confirming that origin is
  genuinely non-responsive — never because the primary origin returned the SPA.

Filesystem:
- {{REPO_PATH}}/ (read only)
- {{REPO_PATH}}/.storron/deliverables/ (read-write)
- {{REPO_PATH}}/.storron/scratchpad/ (read-write) - screenshots, scripts, scratch work, etc.
